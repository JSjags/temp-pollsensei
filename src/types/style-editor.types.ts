export interface FontOption {
  value: string;
  label: string;
  className?: string;
}

export interface SizeOption {
  value: number;
  label: number;
}

export interface FontSettings {
  name: string;
  size: number;
}
