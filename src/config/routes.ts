export default Object.freeze({
  SINGLE_ARTICLE_PAGE: (slug: string) => `/resource-hub/articles/${slug}`,
});

export const learningHubRoutes = Object.freeze({
  SINGLE_ARTICLE_PAGE: (slug: string) => `/learning-hub/articles/${slug}`,
});
