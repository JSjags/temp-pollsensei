"use client";

import ProfilePage from "@/subpages/settings/ProfilePage";
import React from "react";

type Props = {};

const Page = (props: Props) => {
  return <ProfilePage />;
};

export default Page;
