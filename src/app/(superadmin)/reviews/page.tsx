import AdminReviews from "@/subpages/superadmin/AdminReviews";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return(<div>
    <AdminReviews />
  </div>);
};

export default page;
