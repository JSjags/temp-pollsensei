"use client"

import WebArticle from "@/subpages/superadmin/WebArticle";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <WebArticle />;
};

export default page;
