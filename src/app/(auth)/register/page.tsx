"use client";

import RegisterPage from "@/subpages/auth/RegisterPage";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <RegisterPage />;
};

export default page;
