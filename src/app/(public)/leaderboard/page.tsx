import LeaderboardPage from "@/components/publicPages/LeaderboardPage";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <LeaderboardPage />;
};

export default page;
